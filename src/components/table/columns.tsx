import type { TableProps } from "antd";
import { Typography, Button } from "antd";

export interface DataType {
  key: string;
  name: string;
  address: string;
  price: number;
  rating: number;
  pastAppRate: number;
}
